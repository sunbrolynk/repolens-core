# Supply-chain hardening: never run install-time scripts implicitly.
# Run legitimate postinstall steps explicitly (e.g. npx prisma generate).
ignore-scripts=true
# Fail rather than silently mutate the lockfile.
save-exact=true
